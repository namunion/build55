name: Auto Restart + KeepAlive + Run Nexus

on:
  schedule:
    - cron: "*/15 * * * *"   # Kiểm tra mỗi 15 phút
  workflow_dispatch:

jobs:
  maintain:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ""   # 🧹 Xóa GH_TOKEN mặc định ở toàn job để tránh xung đột

    steps:
      - name: 🧰 Cài đặt công cụ cần thiết
        run: |
          sudo apt update -y
          sudo apt install -y gh jq curl

      - name: 🔐 Đăng nhập GitHub CLI bằng PERSONAL_TOKEN
        env:
          PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
          GH_TOKEN: ""  # Đảm bảo không dùng token mặc định
        run: |
          echo "Đăng nhập GitHub CLI bằng token cá nhân..."
          echo "$PERSONAL_TOKEN" | gh auth login --with-token
          echo "✅ Kiểm tra trạng thái đăng nhập:"
          gh auth status

      - name: 🧩 Tìm Codespace đang hoạt động
        id: detect
        run: |
          name=$(gh codespace list --json name,state,lastUsedAt --jq '.[] | select(.state=="Available") | .name' | head -n 1)
          echo "🧩 Codespace phát hiện: $name"
          echo "name=$name" >> $GITHUB_OUTPUT

      - name: ⏱️ Kiểm tra thời gian hoạt động và restart nếu gần hết hạn
        if: steps.detect.outputs.name != ''
        id: uptime
        env:
          PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
          GH_TOKEN: ""
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          echo "🕒 Kiểm tra thời gian hoạt động của Codespace: $CODE_NAME"

          START_TIME=$(gh codespace list --json name,lastUsedAt --jq ".[] | select(.name==\"$CODE_NAME\") | .lastUsedAt")
          START_EPOCH=$(date -d "$START_TIME" +%s)
          NOW_EPOCH=$(date +%s)
          RUNTIME_MIN=$(( (NOW_EPOCH - START_EPOCH) / 60 ))

          echo "⏱️ Codespace đã chạy $RUNTIME_MIN phút"
          echo "runtime=$RUNTIME_MIN" >> $GITHUB_OUTPUT

          if [ "$RUNTIME_MIN" -ge 355 ]; then
            echo "⚠️ Codespace sắp hết hạn → restart..."
            gh codespace stop --codespace "$CODE_NAME"
            sleep 20
            gh codespace start --codespace "$CODE_NAME"
            echo "🔁 Codespace đã được restart!"
            echo "restarted=true" >> $GITHUB_OUTPUT
          else
            echo "✅ Còn trong giới hạn an toàn."
            echo "restarted=false" >> $GITHUB_OUTPUT
          fi

      - name: 💤 Giữ Codespace luôn hoạt động (Keep-Alive)
        if: steps.detect.outputs.name != ''
        env:
          PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
          GH_TOKEN: ""
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          echo "💤 Gửi keep-alive ping cho Codespace: $CODE_NAME"
          gh codespace ssh --codespace "$CODE_NAME" -- bash -lc '
            mkdir -p /workspaces/build55/keepalive
            (while true; do
              echo "[KeepAlive] Ping $(date)" >> /workspaces/build55/keepalive/ping.log
              sleep 300
            done) >/dev/null 2>&1 &
          '
          echo "✅ Keep-alive background process đã khởi động!"

      - name: 🚀 Khởi động Nexus (tự bỏ qua nếu đang chạy)
        if: steps.detect.outputs.name != ''
        env:
          PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
          GH_TOKEN: ""
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          echo "💻 Kiểm tra tiến trình Nexus trong Codespace: $CODE_NAME"

          RUNNING=$(gh codespace ssh --codespace "$CODE_NAME" -- bash -lc "pgrep -x nexus-network || true")

          if [ -n "$RUNNING" ]; then
            echo "✅ Nexus đang chạy (PID: $RUNNING)"
          else
            echo "⚙️ Nexus chưa chạy — khởi động ở chế độ headless..."
            gh codespace ssh --codespace "$CODE_NAME" -- bash -lc '
              cd /workspaces/build55/nexus-cli && \
              nohup ./target/release/nexus-network start \
                --node-id 37001845 \
                --max-threads 8 \
                --max-difficulty extra_large_5 \
                --headless \
                > /workspaces/build55/nexus-cli/nexus_run.log 2>&1 &
            '
            echo "🚀 Đã khởi động Nexus headless trong Codespace!"
          fi

      - name: ⚡ Tự động khởi động Nexus sau restart
        if: steps.uptime.outputs.restarted == 'true'
        env:
          PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
          GH_TOKEN: ""
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          echo "⚡ Codespace vừa restart, khởi động lại Nexus headless..."
          gh codespace ssh --codespace "$CODE_NAME" -- bash -lc '
            cd /workspaces/build55/nexus-cli && \
            nohup ./target/release/nexus-network start \
              --node-id 37001845 \
              --max-threads 8 \
              --max-difficulty extra_large_5 \
              --headless \
              > /workspaces/build55/nexus-cli/nexus_auto_restart.log 2>&1 &
          '
          echo "✅ Nexus đã tự khởi động lại sau restart!"

      - name: ⚠️ Không có Codespace hoạt động
        if: steps.detect.outputs.name == ''
        run: |
          echo "⚠️ Không tìm thấy Codespace nào đang hoạt động. Hãy mở Codespace trước khi workflow chạy."
