name: Auto Restart Codespace

on:
  schedule:
    - cron: "0 */6 * * *"  # Mỗi 6 tiếng restart Codespace
  workflow_dispatch:

jobs:
  restart:
    runs-on: ubuntu-latest

    steps:
      - name: Install GitHub CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y gh jq

      - name: Authenticate GitHub CLI
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
        run: |
          echo "🔐 Authenticating gh CLI..."
          echo $GH_TOKEN | gh auth login --with-token

      - name: Detect current Codespace name
        id: detect
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
        run: |
          echo "📦 Detecting active Codespace..."
          name=$(gh codespace list --json name,state --jq '.[] | select(.state=="Available") | .name' | head -n 1)
          echo "Detected Codespace: $name"
          echo "name=$name" >> $GITHUB_OUTPUT

      - name: Restart Codespace via API
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
        run: |
          echo "🚀 Restarting Codespace: ${{ steps.detect.outputs.name }}"
          curl -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/user/codespaces/${{ steps.detect.outputs.name }}/start

