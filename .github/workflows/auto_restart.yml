name: Auto KeepAlive + Nexus Runner (Stable Final)

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  maintain:
    runs-on: ubuntu-latest
    env:
      PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}

    steps:
      - name: 🧰 Cài công cụ cần thiết
        run: |
          sudo apt update -y
          sudo apt install -y gh jq curl

      - name: 🔐 Đăng nhập GitHub CLI bằng PERSONAL_TOKEN
        run: |
          echo "🧹 Dọn token cũ..."
          unset GH_TOKEN
          unset GITHUB_TOKEN
          rm -rf ~/.config/gh
          echo "🔑 Đăng nhập GitHub CLI..."
          echo "${PERSONAL_TOKEN}" | gh auth login --hostname github.com --with-token
          echo "✅ Đăng nhập thành công!"
          gh auth status || true

      - name: 🧩 Phát hiện Codespace đang hoạt động
        id: detect
        run: |
          name=$(gh codespace list --json name,state,lastUsedAt --jq '.[] | select(.state=="Available") | .name' | head -n 1)
          if [ -z "$name" ]; then
            echo "⚠️ Không có Codespace nào đang hoạt động!"
          else
            echo "🧩 Codespace phát hiện: $name"
            echo "name=$name" >> $GITHUB_OUTPUT
          fi

      - name: ⏱️ Kiểm tra uptime và restart nếu gần hết hạn
        if: steps.detect.outputs.name != ''
        id: uptime
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          START_TIME=$(gh codespace list --json name,lastUsedAt --jq ".[] | select(.name==\"$CODE_NAME\") | .lastUsedAt")
          START_EPOCH=$(date -d "$START_TIME" +%s)
          NOW_EPOCH=$(date +%s)
          RUNTIME_MIN=$(( (NOW_EPOCH - START_EPOCH) / 60 ))
          echo "🕒 Codespace $CODE_NAME đã chạy $RUNTIME_MIN phút."
          echo "runtime=$RUNTIME_MIN" >> $GITHUB_OUTPUT

          if [ "$RUNTIME_MIN" -ge 355 ]; then
            echo "⚠️ Gần hết hạn — restart..."
            gh codespace stop --codespace "$CODE_NAME"
            sleep 20
            gh codespace start --codespace "$CODE_NAME"
            echo "restarted=true" >> $GITHUB_OUTPUT
          else
            echo "✅ Còn trong giới hạn an toàn."
            echo "restarted=false" >> $GITHUB_OUTPUT
          fi

      - name: 💤 Gửi keep-alive ping
        if: steps.detect.outputs.name != ''
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          echo "💤 Gửi keep-alive ping cho $CODE_NAME"

          KEEPALIVE_CMD='
            set -e
            WORKDIR="/workspaces/build55/keepalive"
            mkdir -p "$WORKDIR"
            echo "[KeepAlive] $(date)" >> "$WORKDIR/ping.log"
            echo "Ping OK -> $WORKDIR/ping.log"
          '

          gh codespace ssh --codespace "$CODE_NAME" -- bash --noprofile --norc -c "$KEEPALIVE_CMD" || echo "⚠️ Ping không thành công (bỏ qua lỗi)."
          echo "✅ Keep-alive ping gửi thành công!"

      - name: 🚀 Kiểm tra & khởi động Nexus nếu chưa chạy
        if: steps.detect.outputs.name != ''
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          echo "💻 Kiểm tra tiến trình Nexus..."

          CHECK_CMD='
            RUNNING=$(pgrep -f nexus-network || true)
            if [ -n "$RUNNING" ]; then
              echo "✅ Nexus đang chạy (PID: $RUNNING)"
            else
              echo "⚙️ Nexus chưa chạy — khởi động headless..."
              cd /workspaces/build55/nexus-cli
              nohup ./target/release/nexus-network start \
                --node-id 37001845 \
                --max-threads 8 \
                --max-difficulty extra_large_5 \
                --headless > /workspaces/build55/nexus-cli/nexus_headless.log 2>&1 &
              echo "🚀 Nexus headless đã khởi động!"
            fi
          '

          gh codespace ssh --codespace "$CODE_NAME" -- bash --noprofile --norc -c "$CHECK_CMD"

      - name: ⚡ Tự động khởi động Nexus sau restart
        if: steps.uptime.outputs.restarted == 'true'
        run: |
          CODE_NAME=${{ steps.detect.outputs.name }}
          echo "⚡ Restart xong → khởi động lại Nexus..."

          RESTART_CMD='
            cd /workspaces/build55/nexus-cli
            nohup ./target/release/nexus-network start \
              --node-id 37001845 \
              --max-threads 8 \
              --max-difficulty extra_large_5 \
              --headless > /workspaces/build55/nexus-cli/nexus_auto_restart.log 2>&1 &
            echo "✅ Nexus đã khởi động lại sau restart!"
          '

          gh codespace ssh --codespace "$CODE_NAME" -- bash --noprofile --norc -c "$RESTART_CMD"
